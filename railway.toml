[build]
builder = "NIXPACKS"
# npm ci radi Nixpacks install faza; ovde samo build
buildCommand = "npm run build"

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
